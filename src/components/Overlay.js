import React from "react";

const style = {
	fontSize: "50px",
};

const Overlay = props => (
	<div className="overlay full-page" style={style}>
		This is an overlay.
	</div>
);
export default Overlay;
